{
  "name": "desensitize-mousemove",
  "description": "A wrapper for mousemove callback that allows to adjust hook-in sensitivity.",
  "license": "ISC",
  "version": "0.1.0",
  "scripts": {
    "autoformat": "prettier --trailing-comma es5 --write './src/**/*.{js,jsx}'",
    "build": "babel ./src/ --out-dir ./dist"
  },
  "main": "dist/index.js",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
